import React,{useState} from "react"; import {Link,useNavigate} from "react-router-dom"; import {Leaf,User,Mail,Lock,Tractor} from "lucide-react"; import {useAuth} from "../context/AuthContext";
export default function Register(){const nav=useNavigate();const {register,loading}=useAuth();const [form,setForm]=useState({name:"",email:"",password:"",role:"buyer"});const [error,setError]=useState("");const set=(k,v)=>setForm(f=>({...f,[k]:v}));const submit=async e=>{e.preventDefault();setError("");try{const u=await register(form);nav(u.role==="farmer"?"/farmer":"/buyer")}catch(e){setError(e.message)}};return <div className="auth-page"><div className="auth-brand"><Leaf/> FarmDirect</div><form className="auth-card wide" onSubmit={submit}><span className="eyebrow">JOIN FARMDirect</span><h1>Create account</h1><p>Start buying or selling fresh produce.</p>{error&&<div className="error-box">{error}</div>}<div className="two-col"><div><label>Full name</label><div className="auth-input"><User size={17}/><input required value={form.name} onChange={e=>set("name",e.target.value)} placeholder="Your name"/></div></div><div><label>Account type</label><div className="auth-input"><Tractor size={17}/><select value={form.role} onChange={e=>set("role",e.target.value)}><option value="buyer">Buyer</option><option value="farmer">Farmer</option></select></div></div></div><label>Email</label><div className="auth-input"><Mail size={17}/><input required type="email" value={form.email} onChange={e=>set("email",e.target.value)} placeholder="you@example.com"/></div><label>Password</label><div className="auth-input"><Lock size={17}/><input required minLength={6} type="password" value={form.password} onChange={e=>set("password",e.target.value)} placeholder="Create a password"/></div><button className="primary-btn full" disabled={loading}>{loading?"Creating…":"Create Account"}</button><div className="auth-footer">Already registered? <Link to="/login">Sign in</Link></div></form></div>}
